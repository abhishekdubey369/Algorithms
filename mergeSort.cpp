#include <iostream>

using namespace std;

class mergeSort {

  private:
    int* Arr;         // Pointer to the array
    int size;         // size of an Array
    int capacity;     // Dynamicness of Array

  public:
    mergeSort(int* A)
  
}
